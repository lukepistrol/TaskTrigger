name: lint
on: push

jobs:
  lint:
    runs-on: macos-14
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3
      - name: Run SwiftLint
        run: |
          swiftlint --strict
